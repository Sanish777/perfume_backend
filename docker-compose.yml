version: "3.8"

services:
  app:
    build: .
    container_name: nodejs_app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DB_HOST: 172.17.19.160
      DB_USER: perfume_user
      DB_PASSWORD: perfume_password
      DB_NAME: perfume_db